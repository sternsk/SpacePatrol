{
  "scripts": {
    "build": "node esbuild.config.js",
    "esbuild-base": "esbuild ./src/index.ts --bundle --outdir=dist --format=esm --platform=browser",
    "esbuild": "npm run esbuild-base -- --sourcemap",
    "esbuild-watch": "npm run esbuild-base -- --sourcemap --watch",
    "test-compile": "tsc -p ./"
  },
  "dependencies": {
    "midi-parser-js": "^4.0.4",
    "midi-player-js": "^2.0.16"
  },
  "devDependencies": {
    "esbuild": "^0.21.4",
    "typescript": "^4.6.3"
  }
}

